import Skaside from "@/skeletion/SkAside";
import SkMain from "@/skeletion/SkMain";

const page = () => {
  return (
    <section>
      <SkMain />
      <Skaside />
    </section>
  );
};

export default page;
